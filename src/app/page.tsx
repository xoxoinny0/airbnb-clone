import { Box, BoxProps, styled } from "@mui/material";
import StyledCustomization from "./_component/Slider";

const Box2 = styled(Box)``;

export default function Home() {
  return (
    <div>
      <div>home</div>
      <StyledCustomization />
    </div>
  );
}
